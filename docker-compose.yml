# version: '3.8'

services:
  cleansales_api:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "8888:8000"
    volumes:
      - ./src:/app/src
      - ./data:/app/data
      # - ./src:/app/src
      # - ./tests:/app/tests
      # - ./pyproject.toml:/app/pyproject.toml
      # - ./README.md:/app/README.md
    # environment:
      # - UV_SYSTEM_PYTHON=1
      # - PYTHONPATH=/app
      # - PYTHONUNBUFFERED=1
    restart: unless-stopped
    command:  uv run cleansales-api
    # command: uv pip install --system -e .
    # command: sh -c "uv pip install --system -e . && uv run cleansales-api"
    # command: sh -c "uv pip install --system -e . && uv run cleansales"
    # command: uv run cleansales-api
    # command: uv run cleansales



  #     context: .
  #     dockerfile: Dockerfile.dev
  #   command: uv run cleansales
