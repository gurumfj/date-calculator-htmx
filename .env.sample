# Date Calculator HTMX Configuration
# Copy this file to .env and modify the values as needed

# Session Security - IMPORTANT: Change this in production!
# Generate a secure random key: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-this-in-production

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Development/Production Mode
DEBUG=false

# Session Configuration
# SESSION_MAX_AGE=86400  # Session lifetime in seconds (default: 1 day)

# Optional: CORS Settings (if needed for API access)
# CORS_ORIGINS=["http://localhost:3000", "https://yourdomain.com"]

# Optional: Database (for future persistence features)
# DATABASE_URL=sqlite:///./date_calculator.db